{
  "name": "@gerald/auditor-worker",
  "version": "0.1.0",
  "private": true,
  "description": "Maude McLedger — the AI Auditor of Who Ate Gerald? Cloudflare Worker + Durable Object holding the auditor key: decrypts confidential transfers in code, lets the model phrase (never invent) the answers.",
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "types": "wrangler types",
    "typecheck": "tsc -p tsconfig.json",
    "tail": "wrangler tail gerald-auditor"
  },
  "dependencies": {
    "openai": "^6.43.0",
    "@noble/curves": "^1.9.4",
    "@noble/hashes": "^1.8.0",
    "@ctd/sdk": "*"
  },
  "devDependencies": {
    "wrangler": "^4.114.0",
    "typescript": "^5.6.0"
  }
}
