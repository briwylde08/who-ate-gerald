{
  "name": "who-ate-gerald",
  "version": "0.1.0",
  "private": true,
  "engines": {
    "node": ">=20"
  },
  "description": "Trust is scarce. Gerald is dead. \u2014 a social-deduction game on Stellar confidential tokens",
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "build:sdk": "npm run build --workspace @ctd/sdk",
    "deploy:stack": "tsx scripts/deploy-stack.ts",
    "setup:shops": "tsx scripts/setup-shops.ts",
    "health": "tsx scripts/health.ts",
    "test:catalog": "tsx scripts/test-catalog.ts",
    "test:auditor": "tsx scripts/test-auditor-facts.ts",
    "setup:order": "tsx scripts/setup-order.ts",
    "item-test": "tsx scripts/item-test.ts",
    "bots": "tsx scripts/bots.ts",
    "test:auth": "tsx scripts/test-auth.ts",
    "deploy:app": "npm run build --workspace @gerald/app && wrangler pages deploy packages/app/dist --project-name who-ate-gerald --branch main",
    "check": "npm run types -w @gerald/auditor-worker && tsc -p packages/app/tsconfig.json --noEmit && tsc -p packages/auditor-worker/tsconfig.json --noEmit && tsc -p scripts/tsconfig.json",
    "bank-tills": "tsx scripts/bank-tills.ts"
  },
  "devDependencies": {
    "tsx": "^4.19.0",
    "typescript": "^5.6.0"
  },
  "type": "module"
}
